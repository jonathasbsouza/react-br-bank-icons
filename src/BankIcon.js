import React from 'react';
import { propTypes, defaultProps } from './types';

const BankIcon = () => <div>Hello!</div>;

BankIcon.propTypes = propTypes;
BankIcon.defaultProps = defaultProps;

export default BankIcon;
